sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note and clicks "Save"

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created (JSON confirmation)
    deactivate server

    Note right of browser: The browser updates the notes list locally using JavaScript

    Note right of browser: No page reload happens, the new note appears immediately
